import styled from 'styled-components'
import { g, layout } from '../../../../core'

export const Steps = styled.div`
  margin: 0 auto;
  max-width: ${layout.desktop.default};
  padding: 0 ${g(2)};
`
