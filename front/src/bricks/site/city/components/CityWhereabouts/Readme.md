### Import

```js static
import { CityWhereabouts } from 'bricks'
```

### Usage

```html
<CityWhereabouts {...props} />
```

### Example

```jsx
const props = {
  cities: [
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    },
    {
      slug: 'city-slug',
      region_slug: 'region-slug',
      city_name: 'Lyon'
    }
  ]
}
;<CityWhereabouts {...props} />
```
