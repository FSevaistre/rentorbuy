import styled from 'styled-components'

import { g } from '../../../../core'

export const Block = styled.p`
  margin-bottom: ${g(3)};

  &:last-child {
    margin-bottom: 0;
  }
`

export const Title = styled.div``
