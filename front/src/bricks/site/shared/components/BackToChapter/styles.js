import styled from 'styled-components'

import { g } from '../../../../core'

export const Back = styled.div`
  align-items: center;
  display: flex;
`

export const Title = styled.div`
  margin-left: ${g(2)};
`
