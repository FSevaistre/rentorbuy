import styled from 'styled-components'
import { g } from '../../bricks/core'

export const Graph = styled.div`
  display: flex;
  justify-content: center;
  padding: ${g(2)};
`
export const Equilibrium = styled.p`
  text-align: center;
  margin: ${g(2)};
`
